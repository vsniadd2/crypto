# 🚀 Crypto Analysis Platform

Современная веб-платформа для анализа и прогнозирования криптовалют с использованием искусственного интеллекта и технического анализа.

---

## 📋 Описание проекта

**Crypto Analysis Platform** — это полнофункциональное веб-приложение для мониторинга, анализа и прогнозирования криптовалютных активов. Платформа объединяет данные из внешних API, технический анализ, локальную LLM-модель для генерации прогнозов и систему новостей.

### 🎯 Основные возможности

- 💹 **Мониторинг криптовалют** — Real-time обновления курсов и рыночных данных
- 📊 **Технический анализ** — Расчет RSI, SMA, уровней поддержки/сопротивления
- 🤖 **AI-прогнозирование** — Интеграция с локальной LLM (Ollama) для генерации прогнозов
- 📈 **Визуализация данных** — Графики и диаграммы криптовалют
- 📰 **Новостная лента** — Управление новостями о криптовалютах
- 👤 **Система пользователей** — Регистрация, аутентификация, профили
- ⚡ **WebSocket** — Real-time обновления через WebSocket
- 🔐 **Безопасность** — JWT токены, Spring Security

---

## 🛠 Технологический стек

### Backend
- **Java 21** — Основной язык программирования
- **Spring Boot 3.2.3** — Фреймворк для разработки
- **Spring Security** — Аутентификация и авторизация
- **Spring Data JPA** — Работа с базой данных
- **Spring WebSocket** — Real-time коммуникация

### База данных
- **PostgreSQL** — Основная реляционная БД
- **Redis** — Кэширование и сессии
- **Flyway** — Миграции базы данных

### AI/ML
- **Ollama** — Локальная LLM для анализа и прогнозирования
- **Llama 3.2:3b** — Модель для генерации прогнозов

### API интеграции
- **CoinGecko API** — Данные о криптовалютах
- **CoinMarketCap API** — Дополнительные рыночные данные
- **Bybit API** — Торговые данные

### Frontend
- **Thymeleaf** — Серверный шаблонизатор
- **HTML/CSS/JavaScript** — Клиентская часть

### Инструменты сборки
- **Gradle** — Система сборки проекта

### Дополнительно
- **JWT** — JSON Web Tokens для аутентификации
- **Spring Mail** — Отправка электронной почты
- **Spring Actuator** — Мониторинг приложения

---

## 📦 Структура проекта

```
crypto/
├── src/
│   ├── main/
│   │   ├── java/com/difbriy/web/
│   │   │   ├── auth/              # Аутентификация
│   │   │   ├── config/            # Конфигурация (Security, Redis, WebSocket)
│   │   │   ├── controller/        # REST контроллеры
│   │   │   ├── dto/               # Data Transfer Objects
│   │   │   ├── entity/            # JPA сущности
│   │   │   ├── exception/         # Обработка исключений
│   │   │   ├── handler/           # Exception handlers
│   │   │   ├── locallm/           # LLM сервисы
│   │   │   ├── mapper/            # Маппинг объектов
│   │   │   ├── repository/        # JPA репозитории
│   │   │   ├── roles/             # Роли пользователей
│   │   │   ├── service/           # Бизнес-логика
│   │   │   ├── token/             # JWT токены
│   │   │   ├── utils/             # Утилиты
│   │   │   └── WebApplication.java
│   │   └── resources/
│   │       ├── application.yml    # Конфигурация приложения
│   │       ├── db/migration/      # SQL миграции Flyway
│   │       └── templates/         # HTML шаблоны
│   └── test/                      # Тесты
├── build.gradle                   # Конфигурация Gradle
├── docker-compose.yml             # Docker конфигурация
└── README.md                      # Документация
```

---

## 🚀 Быстрый старт

### Требования

- **Java 21+**
- **PostgreSQL 12+**
- **Redis 6+**
- **Gradle 7+**
- **Ollama** (опционально, для AI-прогнозов)

### Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd crypto
   ```

2. **Настройте базу данных PostgreSQL:**
   ```sql
   CREATE DATABASE difbri_db;
   ```

3. **Настройте Redis:**
   - Убедитесь, что Redis запущен на `localhost:6379`

4. **Обновите конфигурацию:**
   Отредактируйте `src/main/resources/application.yml`:
   ```yaml
   spring:
     datasource:
       url: jdbc:postgresql://localhost:5432/difbri_db
       username: postgres
       password: your_password
   ```

5. **Запустите миграции Flyway:**
   Миграции выполняются автоматически при запуске приложения.

6. **Запустите приложение:**
   ```bash
   ./gradlew bootRun
   # или
   gradlew.bat bootRun
   ```

7. **Откройте в браузере:**
   ```
   http://localhost:8080
   ```

---

## 🔧 Конфигурация

### Настройка API ключей

Обновите `application.yml` с вашими API ключами:

```yaml
api:
  crypto:
    key: your_coinmarketcap_api_key
    
bybit:
  api:
    key: your_bybit_api_key
```

### Настройка LLM (Ollama)

1. Установите Ollama: https://ollama.ai/
2. Загрузите модель:
   ```bash
   ollama pull llama3.2:3b
   ```
3. Запустите Ollama сервер (по умолчанию на `localhost:11434`)

Конфигурация в `application.yml`:
```yaml
local:
  llm:
    base-url: http://localhost:11434
    model: llama3.2:3b
    enabled: true
```

### Настройка почты

Обновите параметры SMTP в `application.yml`:
```yaml
spring:
  mail:
    username: your_email@gmail.com
    password: your_app_password
```

---

## 📚 API Endpoints

### Аутентификация
- `POST /api/auth/register` — Регистрация пользователя
- `POST /api/auth/login` — Вход в систему

### Криптовалюты
- `GET /api/crypto` — Получить список криптовалют
- `GET /api/crypto/{id}` — Получить данные о криптовалюте
- `GET /api/crypto/chart/{symbol}` — Получить график

### Анализ
- `POST /api/crypto/analyze` — Анализ криптовалюты
- `POST /api/predictions/generate` — Генерация прогноза
- `GET /api/predictions/latest/{symbol}` — Последний прогноз
- `GET /api/predictions/history/{symbol}` — История прогнозов

### Новости
- `GET /api/news` — Список новостей (с пагинацией)
- `GET /api/news/{id}` — Новость по ID
- `POST /api/news` — Создать новость (ADMIN)

### Профиль
- `GET /api/profile` — Профиль пользователя
- `PUT /api/profile` — Обновить профиль

### WebSocket
- `/topic/crypto` — Подписка на обновления криптовалют
- `/topic/crypto/response` — Ответы на запросы

---

## 🗄 База данных

### Основные сущности

- **User** — Пользователи системы
- **CryptoData** — Данные о криптовалютах
- **CryptoAnalysis** — Анализ криптовалют
- **CryptoForecast** — Прогнозы цен
- **CryptoPrediction** — AI-прогнозы
- **News** — Новости

Миграции базы данных находятся в `src/main/resources/db/migration/` и выполняются автоматически через Flyway.

---

## 🔐 Безопасность

- **JWT аутентификация** — Токены для доступа к API
- **Spring Security** — Защита эндпоинтов
- **Роли пользователей** — USER, ADMIN
- **CORS** — Настроен для работы с фронтендом

---

## 📊 Функциональность

### Технический анализ
- **RSI** (Relative Strength Index)
- **SMA** (Simple Moving Average) — 20, 50, 200 периодов
- Уровни поддержки и сопротивления
- Волатильность

### AI-прогнозирование
- Использование локальной LLM (Llama 3.2)
- Анализ рыночных данных
- Генерация прогнозов на различные временные периоды
- Учет новостей и технических индикаторов

### Real-time обновления
- WebSocket для мгновенных обновлений курсов
- Периодическое обновление данных (каждые 30 секунд)
- Кэширование в Redis для производительности

---

## 🧪 Тестирование

Запуск тестов:
```bash
./gradlew test
```

---

## 📝 Лицензия

Этот проект создан для образовательных целей.

---

## 👥 Авторы

Разработано командой **Difbriy**

---

## 🙏 Благодарности

- **CoinGecko** — за бесплатный API для данных о криптовалютах
- **Ollama** — за локальные LLM модели
- **Spring Boot** — за отличный фреймворк

---

## 📞 Контакты

По вопросам и предложениям создавайте Issues в репозитории.

---

<div align="center">

**Сделано с ❤️ для криптосообщества**

</div>

